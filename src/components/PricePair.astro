---
interface Props {
  price: string;
  priceLabel: string;
  priceNote: string;
  ariaLabel?: string;
  compact?: boolean;
}

const { price, priceLabel, priceNote, ariaLabel, compact = false } = Astro.props;
---

<dl class:list={['price-pair', { 'price-pair--compact': compact }]} aria-label={ariaLabel}>
  <div>
    <dt>{priceLabel}</dt>
    <dd>{price}</dd>
    <small>{priceNote}</small>
  </div>
</dl>

<style>
  .price-pair {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    margin: 22px 0 0;
    border-block: 1px solid var(--line);
  }

  .price-pair > div {
    padding: 18px 18px 18px 0;
  }

  dt {
    color: var(--muted);
    font-size: .76rem;
    font-weight: 700;
    letter-spacing: .08em;
    text-transform: uppercase;
  }

  dd {
    margin: 4px 0 2px;
    font-family: var(--font-serif);
    font-size: clamp(2rem, 3vw, 2.65rem);
    line-height: 1;
  }

  small {
    color: var(--muted);
    font-size: .76rem;
  }

  .price-pair--compact {
    margin-top: 12px;
  }

  .price-pair--compact > div {
    padding-block: 12px;
  }

  .price-pair--compact dd {
    font-size: clamp(1.45rem, 2vw, 1.8rem);
  }

  .price-pair--compact dt,
  .price-pair--compact small {
    font-size: .7rem;
  }

  @media (max-width: 480px) {
    .price-pair {
      grid-template-columns: minmax(0, 1fr);
    }

    .price-pair > div {
      min-width: 0;
      padding: 15px 14px;
    }

    dd {
      font-size: clamp(1.7rem, 10vw, 2.15rem);
    }

    small {
      display: block;
      line-height: 1.35;
    }
  }
</style>
