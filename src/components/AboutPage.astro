---
import { aboutCopy, aboutMedia } from '../lib/about';
import type { Locale } from '../lib/locales';

interface Props { lang: Locale; }
const { lang } = Astro.props;
const copy = aboutCopy[lang];
const imageAlt = {
  what: {
    en: 'People sharing movement and attention in a Contact Improvisation practice',
    ro: 'Oameni care împărtășesc mișcare și atenție într-o practică de Improvizație de contact',
    ru: 'Люди разделяют движение и внимание в практике Контактной импровизации',
  },
  why: {
    en: 'Performers sharing movement and attention on stage',
    ro: 'Performeri care împărtășesc mișcare și atenție pe scenă',
    ru: 'Перформеры разделяют движение и внимание на сцене',
  },
};
const registrationUrl = 'https://docs.google.com/forms/d/e/1FAIpQLSdkDT1NjKXxzbAg_8dqSLEDnYPstOaBZ8oE6mxqNHkLTUbBlg/viewform?usp=header';
const vimeoPlayerUrl = 'https://player.vimeo.com/video/1211155266?title=0&byline=0&portrait=0';
const organiserGroups = [
  {
    label: copy.organisers.coreGroup,
    items: [
      { names: ['Alexandra Soshnicova', 'Sergey Golovnea'], links: ['https://www.facebook.com/alexandra.soshnicova', 'https://www.facebook.com/golovnea'], role: copy.organisers.dance },
      { names: ['Anriana Celan'], links: ['https://www.facebook.com/adriana.celan'], role: copy.organisers.manager },
    ],
  },
  {
    label: copy.organisers.partnersGroup,
    items: [
      { names: [copy.organisers.festivalTeam], links: ['https://www.facebook.com/fest.contactplus'], role: '' },
      { names: [copy.organisers.community], links: ['https://www.facebook.com/contempdance/'], role: '' },
    ],
  },
  {
    label: copy.organisers.supportGroup,
    items: [
      { names: ['Stan Potoku'], links: ['https://www.facebook.com/stanpotoku'], role: copy.organisers.musician },
      { names: ['Olyga Braga', 'Tara Diamondbreath'], links: ['https://www.facebook.com/oliga.braga', 'https://www.facebook.com/bodhi.tara'], role: copy.organisers.support },
    ],
  },
];
---

<main class="about-page">
  <header class="about-hero">
    <div class="about-hero__copy">
      <p class="eyebrow">{copy.strapline}</p>
      <h1>{copy.title}</h1>
      <p class="lead">{copy.lead}</p>
      <p class="about-hero__facts">{copy.date}<span aria-hidden="true">·</span>{copy.place}</p>
    </div>
    <div class="about-hero__media about-media--hero">
      <video data-autoplay-video autoplay muted loop playsinline preload="metadata" poster={aboutMedia.heroPoster} aria-label={copy.video.heroLabel}>
        <source src={aboutMedia.heroVideo} type="video/mp4" />
      </video>
    </div>
  </header>

  <section class="about-section about-section--what" aria-labelledby="about-what">
    <div class="about-section__text"><h2 id="about-what">{copy.what.title}</h2><p class="about-section__lead">{copy.what.text}</p></div>
    <div class="about-media--landscape"><img src={aboutMedia.what} alt={imageAlt.what[lang]} width="2048" height="1365" loading="lazy" decoding="async" /></div>
  </section>

  <section class="about-section about-section--why" aria-labelledby="about-why">
    <div class="about-why__layout">
      <div class="about-why__media about-media--landscape"><img src={aboutMedia.why[4]} alt={imageAlt.why[lang]} width="2048" height="1367" loading="lazy" decoding="async" /></div>
      <div class="about-why__copy"><h2 id="about-why">{copy.why.title}</h2><ul>{copy.why.items.map((item: string) => <li><span class="about-why__marker" aria-hidden="true"></span><span class="about-why__item-text">{item}</span></li>)}</ul></div>
    </div>
  </section>

  <section class="about-section about-section--unfold" aria-labelledby="about-unfold">
    <header><h2 id="about-unfold">{copy.unfold.title}</h2><p>{copy.unfold.text}</p></header>
    <div class="about-unfold__body">
      <div class="about-unfold__chapter about-unfold__structure">
        <div class="about-unfold__intro">
          <h3>{copy.unfold.structureTitle}</h3>
          <p>{copy.unfold.structureText}</p>
        </div>
        <ol class="about-entry-points" aria-label={copy.unfold.structureTitle}>
          {copy.unfold.dimensions.map((item: string, index: number) => <li><span class="about-entry-points__number">0{index + 1}</span><span class="about-entry-points__name">{item}</span><span class="about-entry-points__text">{copy.unfold.dimensionText[index]}</span></li>)}
        </ol>
      </div>
      <div class="about-unfold__chapter about-unfold__rhythm">
        <div class="about-unfold__intro">
          <h3>{copy.day.title}</h3>
          <p>{copy.day.note}</p>
        </div>
        <div class="about-day-arc">
          {[0, 1, 2].map((phase) => <article><span class="about-day-arc__time">{copy.day.phaseTimes[phase]}</span><h4>{copy.day.phaseTitles[phase]}</h4><p>{copy.day.phaseTexts[phase]}</p><ul><li>{copy.day.items[phase * 2]}</li><li>{copy.day.items[phase * 2 + 1]}</li></ul></article>)}
        </div>
      </div>
      <div class="about-unfold__chapter about-festival-film">
        <div class="about-unfold__intro">
          <h3>{copy.video.filmLabel}</h3>
          <p>{copy.video.filmText}</p>
        </div>
        <div class="about-festival-film__media" data-vimeo-shell>
          <iframe src={vimeoPlayerUrl} title={copy.video.vimeoLabel} loading="lazy" allow="autoplay; fullscreen; picture-in-picture" allowfullscreen></iframe>
        </div>
      </div>
    </div>
  </section>

  <section class="about-section about-section--who" aria-labelledby="about-who">
    <div class="about-who__copy"><h2 id="about-who">{copy.who.title}</h2><p>{copy.who.intro}</p><ul>{copy.who.groups.map((item: string) => <li>{item}</li>)}</ul><div class="about-who__entry"><h3>{copy.who.entry}</h3><p>{copy.who.entryText}</p></div></div>
    <div class="about-who__media"><img src={aboutMedia.who[0]} alt="A large group sharing movement in an open space" width="2048" height="1367" loading="lazy" decoding="async" /></div>
  </section>

  <section class="about-section about-section--city" aria-labelledby="about-city"><img src={aboutMedia.city} alt="People moving together outdoors in Chișinău" width="2048" height="1367" loading="lazy" decoding="async" /><div><h2 id="about-city">{copy.city.title}</h2><p class="about-section__lead">{copy.city.text}</p></div></section>

  <section class="about-section about-section--organisers" aria-labelledby="about-organisers">
    <div class="about-organisers__layout">
      <div class="about-organisers__copy">
        <h2 id="about-organisers">{copy.organisers.title}</h2>
        <p class="about-section__lead">{copy.organisers.intro}</p>
        <div class="about-organisers__groups">
          {organiserGroups.map((group) => <section class="about-organisers__group"><h3 class="about-organisers__group-title">{group.label}</h3><div class="about-organisers__items">{group.items.map((organiser) => <p class="about-organisers__item"><span class="about-organisers__names">{organiser.names.map((name, index) => <><a class="about-organisers__link" href={organiser.links[index]} rel="noopener" target="_blank">{name}</a>{index < organiser.names.length - 1 && ', '}</>)}</span>{organiser.role && <small class="about-organisers__role">{organiser.role}</small>}</p>)}</div></section>)}
        </div>
      </div>
      <div class="about-organisers__media about-media--landscape"><img src={aboutMedia.organisers} alt="People greeting each other during the festival" width="2048" height="1365" loading="lazy" decoding="async" /></div>
    </div>
  </section>

  <section class="about-practical" id="passes-participation"><section class="about-section about-section--participation" aria-labelledby="about-participation"><h2 id="about-participation">{copy.participation.title}</h2><p>{copy.participation.text}</p><div class="about-actions"><a class="about-action about-action--primary" href={registrationUrl} rel="noopener" target="_blank">{copy.participation.register}</a></div></section><section class="about-section about-section--faq" aria-labelledby="about-faq"><h2 id="about-faq">{copy.faqTitle}</h2><div class="about-faq">{copy.faq.map(([question, answer]: [string, string], index: number) => <details><summary>{question}</summary>{index === copy.faq.length - 1 ? <><p>{answer}</p><a class="about-faq__action" href={registrationUrl} rel="noopener" target="_blank">{copy.participation.register}</a></> : <p>{answer}</p>}</details>)}</div></section></section>

  <section class="about-explore" aria-labelledby="about-explore"><div><h2 id="about-explore">{copy.explore.title}</h2><nav aria-label={copy.explore.title}><a href={`/${lang}/program`}>{copy.explore.program}</a><a href={`/${lang}/schedule`}>{copy.explore.schedule}</a><a href={`/${lang}/artists`}>{copy.explore.artists}</a></nav></div><img src={aboutMedia.closing} alt="A group of performers sharing a vivid stage moment" width="3600" height="2384" loading="lazy" decoding="async" /></section>
</main>

<script is:inline>
  const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
  document.querySelectorAll('[data-autoplay-video]').forEach((video) => {
    if (reduceMotion.matches) {
      video.removeAttribute('autoplay');
      video.pause();
    }
  });

</script>

<style>
  .about-page {
    width: min(1240px, calc(100% - 40px));
    margin: 0 auto;
    padding: clamp(40px, 5vw, 68px) 0 clamp(22px, 3vw, 38px);
    --about-space-s: clamp(48px, 6vw, 82px);
    --about-space-m: clamp(72px, 7.5vw, 98px);
    --about-space-l: clamp(88px, 9vw, 120px);
    --about-space-xl: clamp(112px, 11vw, 148px);
  }
  .about-hero { margin-bottom: var(--about-space-s); }
  .about-hero__copy { max-width: 810px; margin-bottom: clamp(22px, 3.2vw, 42px); }
  .about-hero h1 { max-width: 760px; margin: 12px 0 18px; }
  .about-hero__facts { display: flex; flex-wrap: wrap; gap: 7px; margin: 24px 0 0; color: var(--accent-muted); font-size: .78rem; font-weight: 650; letter-spacing: .07em; text-transform: uppercase; }
  .about-hero__media, .about-day__media, .about-media--hero, .about-media--landscape, .about-media--portrait, .about-media--sequence, .about-media--video { position: relative; overflow: hidden; background: #222; }
  .about-day__media { background: transparent; }
  .about-media--hero { aspect-ratio: 16 / 9; }
  .about-media--landscape { aspect-ratio: 3 / 2; }
  .about-media--portrait { aspect-ratio: 4 / 5; max-height: min(680px, 72vh); }
  .about-media--sequence { aspect-ratio: 4 / 5; }
  .about-hero video, .about-day video { display: block; width: 100%; height: 100%; object-fit: cover; }
  .about-hero video::-webkit-media-controls-start-playback-button { display: none !important; -webkit-appearance: none; }
  .about-section { margin: var(--about-space-l) 0; }
  .about-section h2, .about-explore h2 { max-width: 680px; margin: 8px 0 20px; font-family: var(--font-serif); font-size: clamp(2rem, 3.8vw, 3.35rem); font-weight: 400; line-height: 1.08; }
  .about-section h3 { margin: 26px 0 12px; font-size: .82rem; letter-spacing: .07em; text-transform: uppercase; }
  .about-section p { max-width: 660px; color: var(--muted); }
  .about-section__lead { font-size: clamp(1.08rem, 1.55vw, 1.32rem); line-height: 1.52; }
  .about-section img, .about-explore img { display: block; width: 100%; height: auto; object-fit: cover; }
  .about-section--what { display: grid; grid-template-columns: minmax(280px, .9fr) minmax(360px, 1.1fr); gap: clamp(34px, 8vw, 122px); align-items: start; padding-top: clamp(18px, 4vw, 54px); }
  .about-section--what h2 { margin-bottom: 26px; }
  .about-section--what > .about-media--landscape { max-height: min(680px, 72vh); }
  .about-section--what > .about-media--landscape img { object-position: center; }
  .about-section--why { margin-top: var(--about-space-l); }
  .about-why__layout { display: grid; grid-template-columns: minmax(0, 7fr) minmax(280px, 5fr); gap: clamp(34px, 7vw, 96px); align-items: center; }
  .about-why__media { aspect-ratio: 3 / 2; }
  .about-why__media img { height: 100%; object-position: center; }
  .about-why__copy h2 { margin-top: 0; }
  .about-why__copy ul { display: block; margin: 24px 0 0; padding: 0; list-style: none !important; }
  .about-why__copy li { padding: 14px 0; border-top: 1px solid var(--line); color: var(--muted); font-size: clamp(1.04rem, 1.28vw, 1.18rem); line-height: 1.42; }
  .about-why__copy li:last-child { border-bottom: 1px solid var(--line); }
  .about-why__marker { display: inline-block; width: 18px; height: 1px; margin-right: .85em; background: var(--accent-muted); vertical-align: .34em; }
  .about-why__image { aspect-ratio: 4 / 5; height: auto; max-height: 560px; }
  .about-section--unfold { margin-top: var(--about-space-xl); padding-bottom: clamp(14px, 2vw, 26px); }
  .about-section--unfold > header { display: block; max-width: 820px; }
  .about-section--unfold > header h2 { margin-bottom: 14px; }
  .about-section--unfold > header p { max-width: 680px; margin: 0; }
  .about-unfold__body { margin-top: clamp(16px, 2.2vw, 26px); }
  .about-unfold__chapter { display: grid; grid-template-columns: minmax(210px, .34fr) minmax(0, 1fr); gap: clamp(34px, 6vw, 80px); padding: clamp(8px, 1.4vw, 14px) 0; }
  .about-unfold__chapter + .about-unfold__chapter { margin-top: clamp(18px, 2.6vw, 34px); }
  .about-unfold__intro { max-width: 310px; }
  .about-unfold__intro h3 { margin: 0 0 12px; color: var(--ink); font-family: var(--font-serif); font-size: clamp(1.45rem, 2.1vw, 2rem); font-weight: 400; letter-spacing: 0; line-height: 1.12; text-transform: none; }
  .about-unfold__intro p { max-width: 310px; margin: 0; color: var(--muted); font-size: .92rem; line-height: 1.48; }
  .about-entry-points { margin: 0; padding: 0; list-style: none; }
  .about-entry-points li { display: grid; grid-template-columns: 38px minmax(190px, .8fr) minmax(240px, 1.2fr); gap: 18px; align-items: baseline; padding: 12px 0; }
  .about-entry-points li + li { border-top: 1px solid rgba(154, 126, 116, .22); }
  .about-entry-points li:first-child { padding-top: 0; }
  .about-entry-points__number, .about-week-flow__number { color: var(--accent-muted); font-size: .72rem; font-weight: 680; letter-spacing: .08em; }
  .about-entry-points__name { color: var(--ink); font-family: var(--font-serif); font-size: clamp(1.15rem, 1.8vw, 1.45rem); }
  .about-entry-points__text { color: var(--muted); font-size: .93rem; line-height: 1.4; }
  .about-day-arc { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: clamp(22px, 3vw, 36px); }
  .about-day-arc article { min-height: 0; padding-top: 0; }
  .about-day-arc__time { color: var(--accent-muted); font-size: .72rem; font-weight: 680; letter-spacing: .08em; text-transform: uppercase; }
  .about-day-arc h4 { margin: 9px 0 8px; color: var(--ink); font-family: var(--font-serif); font-size: clamp(1.2rem, 2vw, 1.62rem); font-weight: 400; line-height: 1.12; }
  .about-day-arc p { margin: 0; color: var(--muted); font-size: .9rem; line-height: 1.38; }
  .about-day-arc ul { display: grid; gap: 4px; margin: 10px 0 0; padding: 0; list-style: none; }
  .about-day-arc li { position: relative; padding: 1px 0 1px 12px; color: var(--ink); font-size: .88rem; line-height: 1.34; }
  .about-day-arc li::before { content: ""; position: absolute; left: 0; top: .86em; width: 3px; height: 3px; border-radius: 999px; background: var(--accent-muted); opacity: .72; }
  .about-festival-film { width: 100%; padding-top: clamp(4px, 1vw, 12px); }
  .about-festival-film__media { position: relative; aspect-ratio: 16 / 9; overflow: hidden; background: #111; }
  .about-festival-film__media iframe { position: absolute; inset: 0; display: block; width: 100%; height: 100%; border: 0; }
  .about-festival-film .about-unfold__intro p:last-child { margin: 0; color: var(--muted); font-size: .92rem; }
  .about-section--who { display: grid; grid-template-columns: minmax(300px, .82fr) minmax(0, 1fr); gap: clamp(34px, 6vw, 86px); align-items: stretch; }
  .about-who__copy { max-width: 560px; }
  .about-who__copy > p { margin: 0 0 26px; line-height: 1.52; }
  .about-who__copy ul { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 6px 24px; margin: 0; padding: 0; list-style: none; }
  .about-who__copy li { padding: 0; color: var(--muted); font-size: .98rem; line-height: 1.52; }
  .about-who__media { justify-self: end; width: min(100%, 680px); height: clamp(480px, 38vw, 590px); aspect-ratio: auto; }
  .about-who__media img { height: 100%; object-position: center 46%; }
  .about-who__entry { max-width: 600px; margin-top: 24px; padding-top: 20px; border-top: 1px solid var(--line); }
  .about-who__entry h3 { margin: 0 0 10px; font-size: .88rem; }
  .about-who__entry p { line-height: 1.55; }
  .about-who__entry p { margin-bottom: 0; }
  .about-section--city { display: grid; grid-template-columns: minmax(360px, 1.1fr) minmax(280px, .9fr); gap: clamp(34px, 8vw, 122px); align-items: start; margin-top: var(--about-space-m); padding-top: clamp(18px, 4vw, 54px); }
  .about-section--city h2 { margin-bottom: 26px; }
  .about-section--city img { aspect-ratio: 3 / 2; max-height: min(680px, 72vh); }
  .about-section--organisers { margin-bottom: var(--about-space-m); padding-top: clamp(12px, 3vw, 38px); }
  .about-organisers__layout { display: grid; grid-template-columns: minmax(280px, .98fr) minmax(320px, 1.02fr); gap: clamp(34px, 6vw, 88px); align-items: stretch; }
  .about-organisers__copy { max-width: 680px; }
  .about-organisers__groups { display: grid; gap: clamp(22px, 2.4vw, 30px); margin-top: clamp(26px, 3.4vw, 38px); padding: 0; }
  .about-organisers__group { display: grid; grid-template-columns: minmax(160px, .4fr) minmax(0, 1fr); gap: clamp(18px, 3vw, 34px); align-items: start; }
  .about-section--organisers .about-organisers__group-title { margin: 0; padding-top: .18em; color: var(--accent-muted); font-size: .84rem; font-weight: 720; letter-spacing: .074em; line-height: 1.3; }
  .about-organisers__items { display: grid; gap: 8px; }
  .about-organisers__item { margin: 0; color: var(--muted); font-size: clamp(1.02rem, 1.04vw, 1.08rem); font-weight: 370; line-height: 1.44; }
  .about-organisers__names { color: var(--muted); font-weight: 390; }
  .about-organisers__link { color: inherit; text-decoration: none; }
  .about-organisers__link:hover, .about-organisers__link:focus-visible { color: inherit; text-decoration: underline; text-decoration-thickness: 1px; text-underline-offset: .18em; }
  .about-organisers__role { display: block; margin-top: 1px; color: color-mix(in srgb, var(--muted) 72%, var(--soft)); font-size: .84em; font-weight: 300; line-height: 1.3; }
  .about-organisers__media { align-self: stretch; width: 100%; height: 100%; max-height: none; margin-top: 0; aspect-ratio: auto; }
  .about-organisers__media img { width: 100%; height: 100%; object-fit: cover; object-position: 64% center; }
  .about-practical { display: grid; grid-template-columns: 1fr; justify-items: center; gap: var(--about-space-l); margin-top: var(--about-space-l); padding: 0; }
  .about-practical .about-section { margin: 0; }
  .about-practical h2 { margin: 0 0 24px; }
  .about-section--participation { justify-self: center; width: min(100%, 760px); max-width: 760px; margin-inline: auto; }
  .about-section--participation p { max-width: 520px; margin: 0; line-height: 1.55; }
  .about-section--faq { justify-self: center; width: min(100%, 760px); max-width: 760px; margin-inline: auto; }
  .about-actions, .about-explore nav { display: flex; flex-wrap: wrap; gap: 12px 24px; margin-top: 22px; }
  .about-action, .about-explore nav a { color: var(--accent-muted); font-weight: 620; text-decoration: none; text-underline-offset: .25em; }
  .about-action { display: inline-flex; align-items: center; min-height: 34px; }
  .about-action--primary { padding: 8px 12px; background: var(--accent-muted); color: var(--paper); font-size: .93rem; }
  .about-action:hover, .about-explore nav a:hover { color: var(--ink); text-decoration: underline; }
  .about-action--primary:hover { background: var(--ink); color: var(--paper); text-decoration: none; }
  .about-action--pending { color: var(--soft); cursor: default; }
  .about-faq { border-top: 1px solid var(--line); }
  .about-faq details { border-bottom: 1px solid var(--line); }
  .about-faq summary { padding: 18px 28px 18px 0; color: var(--ink); font-size: .98rem; font-weight: 560; cursor: pointer; }
  .about-faq summary:hover { background: rgba(145, 106, 94, .07); color: var(--accent-muted); }
  .about-faq summary:focus-visible { background: rgba(145, 106, 94, .07); color: var(--accent-muted); outline: 0; box-shadow: inset 0 0 0 1px var(--accent-muted); }
  .about-faq summary::marker { color: var(--accent-muted); }
  .about-faq p { max-width: 620px; margin: 0 0 12px; color: var(--muted); font-size: .98rem; line-height: 1.5; }
  .about-faq__action { display: inline-flex; margin: 0 0 18px; color: var(--accent-muted); font-size: .94rem; font-weight: 620; text-decoration: none; text-underline-offset: .24em; }
  .about-faq__action:hover, .about-faq__action:focus-visible { color: var(--ink); text-decoration: underline; }
  .about-explore { display: grid; grid-template-columns: 1fr; gap: clamp(26px, 2.8vw, 36px); margin-top: var(--about-space-l); padding-bottom: clamp(8px, 1.8vw, 18px); }
  .about-explore img { aspect-ratio: 3 / 2; object-position: center; }
  .about-page .about-explore > div { display: block; justify-self: center; width: min(100%, 760px); max-width: 760px; margin-inline: auto; order: 2; }
  .about-page .about-explore > img { order: 1; }
  .about-explore h2 { font-size: clamp(2rem, 3vw, 2.85rem); }
  .about-explore nav { margin-top: 18px; gap: 14px 30px; font-size: 1.02rem; }
  @media (min-width: 861px) {
    .about-why__marker { display: none; }
  }
  @media (prefers-reduced-motion: reduce) { .about-hero video { display: none; } .about-hero__media { background: center / cover no-repeat url('/media/about/festival-closing.jpg'); } }
  @media (max-width: 680px) {
    .about-page {
      width: min(100% - 30px, 620px);
      padding-top: 34px;
      padding-bottom: 24px;
      --about-space-s: 48px;
      --about-space-m: 64px;
      --about-space-l: 80px;
      --about-space-xl: 96px;
    }
    .about-hero__copy { margin-bottom: 22px; }
    .about-hero .eyebrow { max-width: none; margin: 0 0 18px; font-size: .7rem; line-height: 1.4; }
    .about-hero h1 { margin: 0 0 16px; font-size: clamp(2.4rem, 10.5vw, 3.05rem); }
    .about-hero .lead { margin: 0; font-size: 1rem; line-height: 1.5; }
    .about-hero__facts { display: block; margin-top: 24px; font-size: .7rem; line-height: 1.4; }
    .about-hero__facts span { margin: 0 .5em; }
    .about-hero video, .about-hero__media { aspect-ratio: 16 / 9; }
    .about-section h2, .about-explore h2 { margin-bottom: 16px; font-size: clamp(1.9rem, 8vw, 2.15rem); line-height: 1.08; }
    .about-section__lead { font-size: 1.02rem; line-height: 1.52; }
    .about-section--what, .about-section--unfold > header, .about-section--day, .about-section--who, .about-section--city, .about-explore, .about-practical { grid-template-columns: 1fr; gap: 24px; }
    .about-section--what { padding-top: 0; }
    .about-section--what h2, .about-section--city h2 { margin-bottom: 18px; }
    .about-section--what > .about-media--landscape { max-height: 72vh; }
    .about-why__layout { grid-template-columns: 1fr; gap: 22px; }
    .about-why__copy { order: -1; }
    .about-why__copy h2 { margin-bottom: 0; }
    .about-why__copy ul { margin-top: 18px; }
    .about-why__copy li,
    .about-why__copy li:last-child { padding: 0; border: 0; font-size: .98rem; line-height: 1.45; }
    .about-why__copy li { display: grid; grid-template-columns: 24px minmax(0, 1fr); gap: 12px; align-items: start; }
    .about-why__marker { display: block; width: 18px; height: 1px; margin: .72em 0 0; background: var(--accent-muted); }
    .about-why__item-text { min-width: 0; }
    .about-why__copy li + li { margin-top: 18px; }
    .about-section--unfold > header h2 { margin-bottom: 10px; }
    .about-unfold__body { margin-top: 22px; }
    .about-unfold__chapter { grid-template-columns: 1fr; gap: 16px; padding: 14px 0; }
    .about-unfold__chapter + .about-unfold__chapter { margin-top: 16px; }
    .about-unfold__intro { max-width: 100%; }
    .about-unfold__intro h3 { font-size: clamp(1.45rem, 6.5vw, 1.9rem); }
    .about-unfold__intro p { max-width: 620px; }
    .about-entry-points li { grid-template-columns: 30px 1fr; gap: 8px 12px; padding: 13px 0; }
    .about-entry-points li:first-child { padding-top: 0; }
    .about-entry-points__text { grid-column: 2; }
    .about-day-arc { grid-template-columns: 1fr; gap: 0; }
    .about-day-arc article { min-height: 0; padding: 0 0 20px; }
    .about-day-arc article:nth-child(n + 2) { padding-top: 22px; border-top: 1px solid rgba(154, 126, 116, .22); }
    .about-day-arc article:last-child { padding-bottom: 0; }
    .about-day-arc__time { display: block; max-width: 78%; margin: 0 auto 7px; font-size: .66rem; font-weight: 620; letter-spacing: .06em; text-align: left; }
    .about-day-arc h4 { max-width: 78%; margin: 0 auto 7px; font-size: 1.24rem; line-height: 1.14; text-align: left; }
    .about-day-arc p { min-height: 0; max-width: 78%; margin-inline: auto; font-size: .9rem; line-height: 1.42; text-align: left; }
    .about-day-arc ul { max-width: 78%; gap: 6px; margin: 12px auto 0; text-align: left; }
    .about-day-arc li { padding: 0 0 0 14px; color: var(--muted); font-size: .88rem; line-height: 1.36; }
    .about-day-arc li::before { top: .68em; left: 0; width: 4px; height: 4px; border-radius: 999px; opacity: .68; }
    .about-unfold__rhythm + .about-festival-film { margin-top: 46px; padding-top: 0; border-top: 0; }
    .about-festival-film { padding-top: 0; }
    .about-who__copy > p { margin-bottom: 22px; font-size: 1rem; line-height: 1.52; }
    .about-who__entry { grid-column: auto; margin-top: 22px; padding-top: 18px; }
    .about-who__copy ul { --who-list-indent: clamp(20px, 5.5vw, 28px); grid-template-columns: 1fr; gap: 8px; width: calc(100% - var(--who-list-indent)); margin-left: var(--who-list-indent); }
    .about-who__media { justify-self: stretch; width: 100%; height: auto; max-height: none; }
    .about-who__media img { height: auto; }
    .about-section--city { padding-top: 0; }
    .about-section--city > div { order: -1; }
    .about-practical { gap: var(--about-space-l); padding: 0; }
    .about-practical h2 { margin-bottom: 14px; }
    .about-section--participation { width: 82%; margin-inline: auto; text-align: left; }
    .about-section--participation p { max-width: 34rem; margin: 0; font-size: .98rem; line-height: 1.5; }
    .about-section--participation .about-actions { justify-content: flex-start; margin-top: 20px; }
    .about-faq { border-top-color: rgba(154, 126, 116, .24); }
    .about-faq details { border-bottom-color: rgba(154, 126, 116, .24); }
    .about-faq summary { display: grid; grid-template-columns: 12px minmax(0, 1fr); gap: 14px; align-items: baseline; padding: 17px 0; font-size: .96rem; font-weight: 520; line-height: 1.38; list-style: none; }
    .about-faq summary::-webkit-details-marker { display: none; }
    .about-faq summary::marker { content: ""; }
    .about-faq summary::before { content: ""; width: 7px; height: 7px; border-right: 1.5px solid var(--accent-muted); border-bottom: 1.5px solid var(--accent-muted); transform: rotate(-45deg) translateY(-1px); transform-origin: center; opacity: .82; }
    .about-faq details[open] summary::before { transform: rotate(45deg) translateY(-2px); }
    .about-faq p { padding-left: 26px; font-size: .94rem; line-height: 1.48; }
    .about-faq__action { margin-left: 26px; }
    .about-practical .about-section--faq { justify-self: start; width: 72%; margin-left: 9%; margin-right: 0; }
    .about-section--faq h2 { text-align: left; }
    .about-organisers__layout { grid-template-columns: 1fr; gap: 24px; }
    .about-organisers__copy { padding-bottom: 0; border-bottom: 0; }
    .about-organisers__groups { gap: 30px; max-width: 88%; margin: 26px auto 0; }
    .about-organisers__group { grid-template-columns: 1fr; gap: 6px; }
    .about-section--organisers .about-organisers__group-title { max-width: 18rem; color: color-mix(in srgb, var(--accent-muted) 86%, var(--ink)); font-size: .86rem; font-weight: 680; letter-spacing: .045em; line-height: 1.28; }
    .about-organisers__items { gap: 9px; padding-left: 14px; }
    .about-organisers__item { font-size: .98rem; line-height: 1.5; }
    .about-organisers__names { display: block; color: var(--muted); font-weight: 390; }
    .about-organisers__names, .about-organisers__role { overflow-wrap: anywhere; }
    .about-organisers__role { margin-top: 2px; color: color-mix(in srgb, var(--muted) 72%, var(--soft)); font-size: .82em; line-height: 1.3; }
    .about-organisers__media { margin-top: 0; max-height: none; aspect-ratio: 3 / 2; }
    .about-organisers__media img { height: auto; }
    .about-explore { gap: 22px; margin-top: var(--about-space-l); padding-bottom: 8px; }
    .about-page .about-explore > div { order: 0; width: 82%; margin-inline: auto; text-align: left; }
    .about-page .about-explore > img { order: 0; }
    .about-explore nav { display: flex; flex-direction: column; align-items: flex-start; gap: 0; margin-top: 16px; font-size: .98rem; }
    .about-explore nav a { display: inline-flex; align-items: center; width: fit-content; min-height: 46px; padding: 14px 6px 14px 0; border-bottom: 1px solid rgba(154, 126, 116, .22); }
    .about-explore nav a:last-child { border-bottom: none; }
  }
  @media (max-width: 860px) {
    .about-why__layout { grid-template-columns: 1fr; gap: 22px; }
    .about-why__copy { order: -1; }
    .about-why__copy ul { margin-top: 18px; }
    .about-why__copy li,
    .about-why__copy li:last-child { padding: 0; border: 0; font-size: .98rem; line-height: 1.45; }
    .about-why__copy li { display: grid; grid-template-columns: 24px minmax(0, 1fr); gap: 12px; align-items: start; }
    .about-why__marker { display: block; width: 18px; height: 1px; margin: .72em 0 0; background: var(--accent-muted); }
    .about-why__item-text { min-width: 0; }
    .about-why__copy li + li { margin-top: 18px; }
  }
  @media (min-width: 600px) and (max-width: 680px) {
    .about-why__layout { grid-template-columns: 1fr; gap: 22px; }
    .about-why__copy h2 { font-size: clamp(1.8rem, 4vw, 2.45rem); }
    .about-entry-points li { grid-template-columns: 30px minmax(150px, .9fr) minmax(170px, 1.1fr); gap: 10px; }
    .about-entry-points__name { font-size: 1.05rem; }
    .about-entry-points__text { font-size: .82rem; }
  }
</style>
